<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
        <link rel="stylesheet" href="/css/main.css" />
        <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600&display=swap" rel="stylesheet" />
    </head>
    <body>
        <%- include('partials/header') %>

        <main class="content-section">
            <h1>Register</h1>
            <p>Create your account to start tracking.</p>

            <% if (errors) { %>
            <div class="alert error">
                <ul>
                    <% errors.forEach(function(error) { %>
                    <li><%= error.msg %></li>
                    <% }); %>
                </ul>
            </div>
            <% } %>

            <form action="/users/register" method="POST" class="styled-form">
                <div class="form-group">
                    <label for="username">Username</label>
                    <input type="text" id="username" name="username" placeholder="Username" required />
                </div>

                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="Password" required />
                    <small style="display: block; margin-top: 5px; color: #aaa">
                        Must be at least 8 chars, 1 uppercase, 1 lowercase, 1 number, 1 special char.
                    </small>
                </div>

                <button type="submit" class="btn-cta">Register</button>
                <p style="margin-top: 20px; text-align: center">
                    Already have an account? <a href="/users/login" style="color: var(--primary)">Login</a>
                </p>
            </form>
        </main>
    </body>
</html>
