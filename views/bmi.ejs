<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
        <link rel="stylesheet" href="/css/main.css" />
        <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600&display=swap" rel="stylesheet" />
    </head>
    <body>
        <%- include('partials/header') %>

        <main class="content-section">
            <h1>BMI Calculator</h1>
            <p>Calculate your Body Mass Index (BMI) to check your health status.</p>

            <form action="/fitness/bmi" method="POST" class="styled-form">
                <div class="form-group">
                    <label for="weight">Weight (kg)</label>
                    <input type="number" id="weight" name="weight" step="0.1" placeholder="e.g. 70" required />
                </div>

                <div class="form-group">
                    <label for="height">Height (cm)</label>
                    <input type="number" id="height" name="height" placeholder="e.g. 175" required />
                </div>

                <button type="submit" class="btn-cta">Calculate BMI</button>
            </form>

            <% if (bmi) { %>
            <div class="styled-form" style="margin-top: 30px; text-align: center">
                <h2>Your BMI is: <%= bmi %></h2>
                <p style="font-size: 1.2rem; color: var(--accent)"><%= status %></p>
            </div>
            <% } %>
        </main>
    </body>
</html>
